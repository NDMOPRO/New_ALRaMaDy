# Phase 58

- Timestamp: 2026-03-16T03:22:52+03:00
- Scope: repair `arabic-localization-lct-engine cross-engine flow proof` so localization intake is sourced from shared dashboard runtime state and downstream publish remains live and governed

## Changed files
- `C:\ALRaMaDy\rasid-platform-core\packages\arabic-localization-lct-engine\src\index.ts`
- `C:\ALRaMaDy\rasid-platform-core\apps\contracts-cli\src\dashboard-web.ts`
- `C:\ALRaMaDy\rasid-platform-core\scripts\smoke\localization-cross-engine-flow-proof.mjs`

## Implemented
- Added a shared-runtime intake builder in `arabic-localization-lct-engine` that derives `source_artifact` and `source_canonical` from live dashboard runtime state.
- Added `POST /api/v1/localization/localize-dashboard` in `dashboard-web` so localization starts from `snapshot(dashboard_id)` and the live state file under `.runtime\dashboard-web\dashboard-engine\dashboards\<id>\state\current.json`.
- Repaired downstream continuity by importing the fresh localized bundle back into `dashboard-web` with localized widget/layout semantics preserved, then publishing it through the live governed `/api/v1/dashboards/publish` route.

## Fresh proof
- Proof root: `C:\ALRaMaDy\rasid-platform-core\packages\arabic-localization-lct-engine\output\xflow-20260316002242\`
- Cross-engine proof: `artifacts\cross-engine-flow-proof.json`
- Evidence: `evidence\evidence.json`
- Audit: `audit\audit.json`
- Lineage: `lineage\lineage.json`

## Tests
- `npx tsc -b tsconfig.json`
- `npm run test:localization-cross-engine-flow`

## Result
- The repaired flow now proves:
  - `report-engine -> dashboard-web shared runtime -> arabic-localization-lct-engine -> dashboard-web publish`
  - localization source-of-truth is the live shared dashboard state file, not `embed-payload.json`
  - the fresh localized artifact is handed downstream into the shared shell and published through the governed live publish route
