[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["cd apps/rasid-web && npm run build"]

[start]
cmd = "cd apps/rasid-web && node dist/index.js"

[variables]
NODE_ENV = "production"
