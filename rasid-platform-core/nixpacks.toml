[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd apps/rasid-web && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd apps/rasid-web && npm run build"]

[start]
cmd = "cd apps/rasid-web && npm run start"

[variables]
NODE_ENV = "production"
PORT = "3000"
