# Phase 29 Report

- Timestamp: 2026-03-15T19:53:52.9804229+03:00
- Scope: Excel engine open-gap closure on formula threading, BIFF8 editable export, strict width fidelity, and desktop pivot behavior proof

## Executed Work

- Updated `rasid-platform-core/packages/excel-engine/src/engine.ts` to:
  - evaluate workbook-linked formulas via worker threads
  - persist formula execution metadata on the run state
  - export legacy `.xls` files through Excel Desktop COM
  - re-import the BIFF8 export and record roundtrip proof
  - repair strict width-fidelity comparison on `Data` and `Joined`
  - persist desktop pivot inspection failure as a proof artifact instead of crashing the regression run
- Added `rasid-platform-core/packages/excel-engine/src/formula-worker.ts` for worker-thread batch evaluation.
- Added `rasid-platform-core/packages/excel-engine/tools/excel_desktop_bridge.ps1` for Excel Desktop COM export/inspection.
- Updated `rasid-platform-core/packages/contracts/src/common.ts` so artifact export refs can declare `export_type = "xls"`.

## Verified Outputs

- Proof root:
  - `C:\ALRaMaDy\rasid-platform-core\packages\excel-engine\output\sample-run-2026-03-15T16-51-35-920Z\`
- Key artifacts:
  - `artifacts/formula-engine-proof.json`
  - `artifacts/fidelity-comparison-proof.json`
  - `artifacts/source-format-proof.json`
  - `artifacts/pivot-desktop-proof.json`
  - `evidence/evidence-pack.json`

## Confirmed Closures From This Run

- Formula engine:
  - `multithreaded_execution.implemented = true`
  - `worker_count = 4`
  - `incremental_recalc_proof.recalculation_state_after_delta = "completed"`
- `.xls` path:
  - real editable export emitted to `artifacts/legacy-editable-output.xls`
  - roundtrip re-import proved with `roundtrip_source_format = "xls"` and `roundtrip_worksheet_count = 14`
- Strict fidelity width repair:
  - `sheet_comparisons[*].matches.column_widths = true` for `Data`, `Summary`, `ArabicSummary`, and `Joined`

## Remaining Open Items From Direct Evidence

- Desktop pivot fidelity:
  - `artifacts/pivot-desktop-proof.json.inspection_status = "failed_to_open"`
  - `pivot_desktop_behavior_check` and `pivot_semantics_refresh_check` remain failed in the current evidence pack
- Native named-style/theme lifecycle is still not proven beyond worksheet/table-style persistence
- Advanced chart-family coverage remains unproven for scatter/bubble/radar/stock
- Universal merge conflict resolution remains unproven beyond the current repeatable policy/sample set
- Backend publication remains a local runtime HTTP/object-store service, not a remote production backend

## Verification

- `npx tsc -b packages/contracts packages/excel-engine` passed
- `npm run test:excel-engine` passed
